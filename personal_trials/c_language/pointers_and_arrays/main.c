#include <stdio.h>

/**
* main - entry point
*
* Return: always 0
*/

int main(void)
{
	/* declare variables and initialize them */
	int a = 4;
	int b = 17;
	char c = 'c';

	/* print the above variables */
	printf("%d %d %c\n", a, b, c);

	return (0);
}
