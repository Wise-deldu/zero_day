#include <stdio.h>

/**
  * main - A program that prints the average of three marks
  *
  * Return: Always 0.
  */

int main(void)
{
	int score1 = 73;
	int score2 = 72;
	int score3 = 33;

	printf("Average: %i\n", (score1 + score2 + score3) / 3);

	return (0);
}
