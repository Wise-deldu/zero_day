#include <stdio.h>

/**
  * main - A program that prints a while loop with a decrement.
  *
  * Return: Always 0.
  */

int main(void)
{
	int m = 10;

	while (m >= 1)
	{
		printf("Make the computer literate!\n");

		m = m - 1;
	}
	return (0);
}
