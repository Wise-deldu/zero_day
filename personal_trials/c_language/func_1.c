#include <stdio.h>

/**
 * main - A code to print the area of a rectangle
 * Return: area (Successful)
 */
int main(void)
{
	int area;

	area = length * breadth;

	int l = 10, b = 5;

	int area = areaOfRect(l, b);

	printf("%d\n", area);
	return (area);
}
